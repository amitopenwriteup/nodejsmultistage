{
  "name": "node-multistage-docker",
  "version": "1.0.0",
  "description": "A simple Node.js app with a multi-stage Dockerfile",
  "main": "server.js",
  "scripts": {
    "start": "node dist/server.js",
    "build": "mkdir -p dist && cp server.js dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}

